import { useEffect } from 'react';

const RegisterMobileNumber = () => {
  useEffect(() => {
    document.title = `Mobile Number | Register | Jambonz`;
  }, []);

  return (
    <h1>Mobile Number</h1>
  );
};

export default RegisterMobileNumber;
